# 팔린아파트 — 프로젝트 인수인계 문서

> 이 문서는 다른 AI 도구/개발자가 이 프로젝트를 이어받기 위한 전체 컨텍스트입니다.
> 작성일: 2026-08-10. 프로젝트 위치: `~/Desktop/Claude/inaptos` (폴더명은 초기 가칭)

---

## 1. 프로젝트가 무엇인가

**팔린아파트** — "호가 말고, 실제로 팔린 아파트 값"

국토교통부 실거래가(공공데이터)를 기반으로 한 부동산 웹앱 + 안드로이드 앱.
네이버부동산 등의 "호가 매물"과 달리, **신고된 실제 계약만** 보여주는 것이 정체성.

- 초기 이름: 인앱토스(오타) → 집한눈 → **팔린아파트** (실거래 중심으로 피벗하며 개명)
- 소유자: 비개발자, 사업자 보유, 구글 개발자 계정(사업자— 테스터 20명 요건 면제), 도메인 보유
- 수익화 방향: 안드로이드 앱 + AdMob 광고 (배너+전면 병행)

## 2. 기술 스택 · 실행

- React 19 + Vite (JavaScript, TypeScript 아님), react-router **HashRouter**(`#/경로`)
- 차트: Recharts / 지도: Leaflet + OpenStreetMap / 앱: **Capacitor 8** (웹뷰 방식)
- 실행: `npm run dev -- --port 5178` / 빌드: `npm run build` / 앱 동기화: `npx cap sync android`

## 3. 데이터 아키텍처 (가장 중요)

### 실데이터 경로
```
브라우저 → Cloudflare Worker 프록시 → 국토부 실거래가 API (XML)
```
- **프록시 URL**: `https://jiphannun-proxy.nolsoop-games.workers.dev`
  - `/apt-trade?lawdCd=11680&dealYmd=202607` (아파트 매매 — 상세/기본 API 순차 시도)
  - `/apt-rent?lawdCd=...&dealYmd=...` (아파트 전월세)
  - 프록시 코드: 저장소의 `cloudflare-worker.js` (v3). 12시간 엣지 캐시
  - 국토부 API 키는 Cloudflare Secret `MOLIT_KEY`에만 존재 (코드에 비밀 없음)
  - **주의**: 키는 Encoding 키가 저장돼 있음 — v3 코드가 `%` 포함 여부로 자동 판별
- **앱 쪽 연동**: `src/data/api.js`
  - `REGION_DISTRICTS`: 서울 25개 구 + 경기 47개 시군구 전체 (name/short/region/lawdCd)
  - `fetchAptTrades`, `fetchRecentAptTrades`(3개월), `fetchAptRents`, `fetchRecentAptRents`(2개월)
  - `fetchPppSeries`(14개월 월평균 평당가 — 전년 동월 비교 위해 14개월)
  - `outlookFromSeries`(규칙 기반 향후 가치 전망 — 모멘텀·거래량만 사용)
  - **모든 fetch에 `cache: 'no-store'` 필수** (과거: 에러 응답이 브라우저에 12시간 캐시돼 고착됐던 사고 있음)
  - 해제된 거래(`cdealType === 'O'`) 제외

### 법정동 코드 함정 (실측으로 확인함)
- **화성시**: 41590은 죽은 코드. 구 신설로 4분할 — 41591(남양·송산) / 41593(봉담) / 41595(병점) / 41597(동탄)
- **부천시**: 41190 죽음 → 41192(원미) / 41194(소사) / 41196(오정)

### 데모 데이터 (폴백 전용)
- `src/data/mock.js` — 시드 고정 생성 데이터. 초기 16개 지역만 존재
- 실데이터 조회 실패 시에만 사용되며, 화면에 반드시 "데모" 배지 + 경고 표시
- 지도 페이지와 매물 상세(데모 매물용)는 아직 데모 (실거래엔 좌표가 없음 — 지오코딩 필요)

## 4. 페이지 구성 (src/pages/)

| 파일 | 경로 | 내용 | 데이터 |
|---|---|---|---|
| Home.jsx | `/` | "최근에 팔린 아파트" — 실계약 카드(매매 3개월+전월세 2개월), 아파트 이름·동 검색, 유형/가격(억 min~max)/면적 필터, 정렬 5종, 30건씩 더보기, 지역 전망 카드, 카드에 준공연도·단지/구 평균 대비 ±%·재건축 연한(30년+) 태그 | **실데이터** |
| Market.jsx | `/market` | 평당가 14개월 차트(진행월 회색), 통계(전월/전년/거래량), 매매 실거래 테이블, 전월세(전세/월세 토글) | **실데이터** |
| MapPage.jsx | `/map` | 지도 가격 마커 | 데모 (배지 표시) |
| Detail.jsx | `/listing/:id` | 데모 매물 상세 | 데모 |
| Calc.jsx | `/calc` | 주담대 한도(LTV70/DSR40)·월상환액·취득세 계산기 | 계산식 |
| Privacy.jsx | `/privacy` | 개인정보처리방침 (스토어·AdMob 필수) | — |

- 지역 선택 UI: 권역 칩(서울/경기) + 구 드롭다운 (사용자가 칩 나열 → 드롭다운으로 변경 요청했음)

## 5. 안드로이드 앱 (Capacitor) — 현재 상태

- `android/` 프로젝트 생성 완료, 웹 빌드 동기화 완료
- **패키지 ID**: `com.nolsoop.apartments` (사용자 확정 — 스토어 첫 업로드 후 변경 불가)
- **AdMob** (`src/ads.js`):
  - 앱 ID: `ca-app-pub-4999376453226791~2203121924` (AndroidManifest.xml에 반영됨)
  - 배너 광고단위: `ca-app-pub-4999376453226791/3492152866` (하단 상시)
  - 전면 광고단위: `ca-app-pub-4999376453226791/8361336167`
  - 전면 노출 규칙: 페이지 이동 시에만, 실행 후 1분 금지(정책) + 이동 3회 이상 + 2분 간격
  - **`IS_TESTING = true` 상태** — 출시 빌드 직전에 false로 바꿀 것 (개발 중 실광고 클릭 = 계정 정지 위험)
  - **웹뷰에 애드센스 삽입은 구글 정책 위반** — 앱 광고는 AdMob만 (이 구조가 그 이유)

### 남은 출시 단계 (이 Mac에 Java/Android Studio 없음 — 소유자 설치 대기 중)
1. Android Studio 설치 (소유자)
2. 에뮬레이터/실기기 테스트 (실거래 조회 + 테스트 광고 확인)
3. `IS_TESTING → false`
4. 서명 키(업로드 키) 생성 + 백업
5. AAB 빌드 → 플레이 콘솔 업로드 (사업자 계정이라 비공개 테스트 의무 없음)
6. 출시 후: AdMob 콘솔에서 스토어 연결 + 앱 심사 (며칠 소요, 그 전엔 광고 안 나옴)

### 보류/미결
- GitHub 배포: 소유자가 보류함
- 웹 배포 + 도메인 연결: 미착수 (도메인은 보유. Cloudflare Pages 추천했음)
- PWA: vite-plugin-pwa 설치만 됨, 설정 안 함 (Capacitor로 방향 전환)

## 6. 작업 원칙 (소유자가 명시적으로 요구한 것 — 반드시 유지)

1. **없는 데이터를 만들어 채우지 말 것.** 사진이 없으면 "등록된 사진이 없습니다"라고 표기.
   실거래 데이터에 없는 것(방 구조·방향·재건축 이슈·주민 리뷰·인프라)은 표시하지 않고 이유를 화면에 고지.
   과거에 플레이스홀더 사진을 만들었다가 "사진 없으면 없다고 솔직히 말해"라고 지적받음.
2. **실데이터/데모 구분을 화면에 항상 표시** ("실데이터"/"데모" 배지, 출처 문구)
3. 전망·시세 지표에는 **"투자 권유 아님" 고지** 필수 (규제 리스크)
4. 소유자는 **비개발자** — 안내는 단계별로, 전문용어 풀어서
5. 이름 관련: "아파트 실거래"는 기존 서비스 '아실'과 겹쳐서 기각됐음

## 7. 알려진 이슈 / 개선 후보

- 홈에서 지역당 요청 5개(매매3+전월세2) + 전망용 14개 = 최초 로드 수 초 (프록시 캐시 후엔 빠름)
- 국토부 API 일 1만 건 제한 — 트래픽 증가 시 프록시 캐시 강화 필요
- 번들 813KB — 코드 스플리팅 여지
- 확장 후보: 빌라(연립다세대)·토지 실거래 API 추가 활용신청, 단지별 상세(거래 이력 모아보기), 지도 실데이터화(지오코딩), 관심 단지 알림(수익모델), 84m² 필터 기반 보정 평당가

## 8. 파일 지도

```
inaptos/
├── HANDOFF.md              ← 이 문서
├── README.md               ← 기능 상세 설명
├── cloudflare-worker.js    ← 프록시 코드 (Cloudflare에 배포된 것과 동일, v3)
├── capacitor.config.json   ← appId, appName
├── android/                ← Capacitor 안드로이드 프로젝트 (AndroidManifest에 AdMob 앱 ID)
├── src/
│   ├── App.jsx             ← 라우팅, 광고 초기화, 푸터(출처 고지)
│   ├── ads.js              ← AdMob 배너+전면 (네이티브 전용, IS_TESTING 플래그)
│   ├── data/
│   │   ├── api.js          ← 실거래 API 연동 + 지역 레지스트리 + 전망 계산 (핵심)
│   │   └── mock.js         ← 데모 데이터 (폴백 전용)
│   ├── pages/              ← Home/Market/MapPage/Detail/Calc/Privacy
│   ├── components/ListingCard.jsx  ← 데모 매물 카드 (실거래 카드는 Home 안 TradeCard)
│   └── index.css           ← 전체 스타일 (토스풍: #3182f6, 카드 radius 16px)
```
